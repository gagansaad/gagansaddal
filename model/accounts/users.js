const mongoose = require("mongoose");
const usersschema = new mongoose.Schema({
    name: {
        type: String,
        required: true
    },name: {
        type: String,
        required: true
    },name: {
        type: String,
        required: true
    }, 

    userbasicInfo:{
        name:{
            type: struing
        }
    }

})